ALTER TABLE `tracks`  ADD `revision_id` INT NULL  AFTER `comment`,  ADD   INDEX  (`revision_id`);
ALTER TABLE `tracks` ADD  FOREIGN KEY (`revision_id`) REFERENCES `revisions`(`id`) ON DELETE CASCADE ON UPDATE NO ACTION;
